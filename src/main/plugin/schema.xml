<schema name="database-query-plugin">
    <table name="jiveDatabaseQueryAudit" description="Table for Auditing the Database Query Plugin">
        <column name="username" type="varchar" nullable="false" description="Username of the user that performed the query"/>
        <column name="databaseUsed" type="varchar" size="20" nullable="false" description="Name of the database that the query was performed against" />
        <column name="queryUsed" type="varchar" size="500" nullable="false" description="Query that was performed against the database" />
        <column name="timestamp" type="bigint" nullable="false" description="Time that the user performed the query" />
    </table>
</schema>